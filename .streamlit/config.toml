# Keep these in step with the CSS custom properties in ui.py's styles().
[theme]
base = "light"
primaryColor = "#12694F"
backgroundColor = "#F6F9F7"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#12211C"
font = "sans serif"
